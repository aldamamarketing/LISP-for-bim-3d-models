rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Por ahora permitimos lectura y escritura a cualquier usuario logueado en la plataforma
      allow read, write: if request.auth != null;
    }
  }
}
