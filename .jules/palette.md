## 2024-05-18 - Missing ID and htmlFor bindings in inputs
**Learning:** Found that AuthLogin forms had visual text near inputs without standard HTML accessibility links.
**Action:** Always link form labels using `htmlFor` with the respective `id` in the `input` field.
